/**
 * Spring wiring that selects shared outbound client implementations.
 *
 * <p>Provider clients contain transport behavior; this package is the single
 * composition point that chooses which provider implements a shared client
 * contract.</p>
 */
package com.trade.client.config;

/**
 * Backend root package.
 *
 * <p>The backend is organized by domain first, then by layer inside each
 * domain. Start with the domain package comments here to find the right entry
 * point before drilling into controllers, schedulers, services, and clients.</p>
 */
package com.trade;

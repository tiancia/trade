<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.trade.ai.persistence.AiResponseParseErrorMapper">
    <insert id="insert"
            parameterType="com.trade.ai.persistence.AiResponseParseErrorRow"
            useGeneratedKeys="true"
            keyProperty="id"
            keyColumn="id">
        INSERT INTO ai_response_parse_errors (
            source,
            phase,
            related_id,
            prompt_text,
            raw_response,
            error_message,
            fallback_action,
            metadata_json
        ) VALUES (
            #{source},
            #{phase},
            #{relatedId},
            #{promptText},
            #{rawResponse},
            #{errorMessage},
            #{fallbackAction},
            #{metadataJson}
        )
    </insert>
</mapper>
